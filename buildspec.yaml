version: 0.2

phases:
  install:
    runtime-versions:
      php: 8.1
  
  pre_build:
    commands:
      - echo "Pre-build steps"

  build:
    commands:

      - echo " MySQL tests"
     

  post_build:
    commands:
      - echo "Post-build steps"
